# Database
# For PostgreSQL with Supabase (Production - Use Pooler for Vercel)
DATABASE_URL="postgresql://postgres.projectref:[password]@aws-0-region.pooler.supabase.com:6543/postgres?pgbouncer=true"

# Direct connection (for migrations only)
DIRECT_URL="postgresql://postgres:[password]@db.projectref.supabase.co:5432/postgres"

# For SQLite (Local Development Only)
# DATABASE_URL="file:./dev.db"

# Authentication
JWT_SECRET="your-secret-key-here-use-openssl-rand-base64-32"

# Email Configuration (Optional)
BREVO_API_KEY="your-brevo-api-key"
BREVO_SENDER_EMAIL="noreply@yourdomain.com"
BREVO_SENDER_NAME="OrderFlow System"

# Area Office Notifications (Optional)
AREA_OFFICE_NOTIFICATION_EMAILS="email1@example.com,email2@example.com"

# Next Auth (if using NextAuth instead of custom JWT)
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-nextauth-secret"